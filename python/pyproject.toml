[project]
name = "gfwlinks"
version = "0.1.0"
description = "Build Global Fishing Watch map URLs without the frontend's encoder"
requires-python = ">=3.8"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Fernando Cagua-Jahn", email = "fernando.cagua@globalfishingwatch.org" },
]
dependencies = []

[project.urls]
Repository = "https://github.com/GlobalFishingWatch/gfwlinks"

[dependency-groups]
dev = ["pytest>=8"]
# live app check only (tests/test_live.py); the package itself stays dependency-free
live = ["pytest>=8", "playwright>=1.40"]

[build-system]
requires = ["uv_build>=0.6,<0.7"]
build-backend = "uv_build"
