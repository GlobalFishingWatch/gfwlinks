% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gfwlinks.R
\name{vessel_profile_url}
\alias{vessel_profile_url}
\title{URL for a single vessel's profile page}
\usage{
vessel_profile_url(
  vessel_id,
  identity_source = "selfReportedInfo",
  visible_events = DEFAULT_EVENTS,
  latitude = NULL,
  longitude = NULL,
  zoom = NULL,
  start = NULL,
  end = NULL
)
}
\arguments{
\item{vessel_id}{Vessel id (the \code{id} field from the GFW API/vessel search).}

\item{identity_source}{Which identity record to show: \code{"selfReportedInfo"}
(default) or \code{"registryInfo"}. Note the live app instead defaults to
\code{"registryInfo"} with a fallback; see DEVELOPMENT.md.}

\item{visible_events}{Character vector of event layers to show, e.g.
\code{"fishing"}, \code{"encounter"}, \code{"port_visit"}, \code{"loitering"}, \code{"gaps"}.
Defaults to all but \code{"loitering"}.}

\item{latitude, longitude, zoom}{Map viewport. All three are optional; when
omitted the map opens at its default view instead of a specific place.}

\item{start, end}{ISO 8601 timestamps (e.g. \code{"2026-01-01T00:00:00.000Z"})
bounding the activity time range shown.}
}
\value{
A character vector of URLs, one per \code{vessel_id}.
}
\description{
Builds the URL for the GFW map page showing one vessel's identity,
activity and (optionally) a viewport centered on a place and time.
}
\details{
Vectorized over every argument (via \code{\link[base:Vectorize]{base::Vectorize()}}), so it can be
called with a column of vessel ids, e.g. \code{mutate(url = vessel_profile_url(vessel_id))}.
\code{visible_events} is the exception: it is passed whole to every call, not
recycled element-by-element, since it's a set of event types rather than
a per-vessel value.
}
\examples{
vessel_profile_url("91da818da-ab9b-1556-e335-ca41831da501")
vessel_profile_url("91da818da-ab9b-1556-e335-ca41831da501",
                    latitude = -43.4, longitude = 176.3, zoom = 8.6)
}
