% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gfwlinks.R
\name{vessel_map_url}
\alias{vessel_map_url}
\title{URL for a map showing one or more vessels' tracks}
\usage{
vessel_map_url(
  vessel_ids,
  latitude = NULL,
  longitude = NULL,
  zoom = NULL,
  start = NULL,
  end = NULL
)
}
\arguments{
\item{vessel_ids}{Character vector of vessel ids to show together.}

\item{latitude, longitude, zoom}{Map viewport. All three are optional; when
omitted the map opens at its default (world) view, since this function
does not compute a fit-bounds around the vessels.}

\item{start, end}{ISO 8601 timestamps (e.g. \code{"2026-01-01T00:00:00.000Z"})
bounding the activity time range shown.}
}
\value{
A single URL (character scalar) showing all of \code{vessel_ids}.
}
\description{
Builds the URL for the GFW fishing-activity map with each vessel added as
its own dataview (distinct colour, identity, track and event layers), and
the default background activity layers (\code{ais}, \code{vms}) hidden so the
vessel tracks aren't buried under them.
}
\examples{
vessel_map_url(c("91da818da-ab9b-1556-e335-ca41831da501",
                  "41a98a2e0-0fbb-3d26-4b71-6d4266443a82"),
                latitude = -43.4, longitude = 176.3, zoom = 8.6)
}
